#define DEF_SUMOKOIN_VERSION_TAG "@VERSIONTAG@"
#define DEF_SUMOKOIN_VERSION "0.4.0.0"
#define DEF_SUMOKOIN_RELEASE_NAME "Akita"
#define DEF_SUMOKOIN_VERSION_FULL DEF_SUMOKOIN_VERSION "-" DEF_SUMOKOIN_VERSION_TAG

#include "version.h"

const char* const SUMOKOIN_VERSION_TAG = DEF_SUMOKOIN_VERSION_TAG;
const char* const SUMOKOIN_VERSION = DEF_SUMOKOIN_VERSION;
const char* const SUMOKOIN_RELEASE_NAME = DEF_SUMOKOIN_RELEASE_NAME;
const char* const SUMOKOIN_VERSION_FULL = DEF_SUMOKOIN_VERSION_FULL;
